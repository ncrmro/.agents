---
title: Short working title
kind: microblog
platform: x
scheduled: null
posted: null
---

<!-- Single post: one paragraph below. Write to the TIGHTEST limit among the
     target channels — look them up in platforms.md, don't recall them. Count
     before scheduling. The first line is all anyone sees in the timeline —
     lead with the claim or payoff, not the setup. No hashtag pile-ups (0-1 is
     plenty); links go at the end or in a reply, not mid-sentence. Link and
     fold behaviour differ per platform; platforms.md has both. -->

[Post text]

<!-- THREAD form: delete the single post above and use numbered sections
     instead, one per post, each within the tightest limit. Post 1 is the
     hook and must stand alone — assume most readers never expand the
     thread. The last post carries the CTA or link. -->

<!-- 1/ -->

<!-- 2/ -->

<!-- MULTI-CHANNEL: this template is for ONE destination. For several, start
     over from template.microblog.multi.md, which carries a `channels:` list
     and a finished variant per channel. Don't retrofit this file. -->

<!-- MEDIA: note any image/video to attach, WITH its alt text — alt text is
     required before scheduling, not optional. The user adds both when
     posting. -->
