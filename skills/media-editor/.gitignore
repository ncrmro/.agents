# devenv build artifacts (devenv.lock is committed for reproducibility)
.devenv/
.devenv.flake.nix
.direnv/

# Local secrets / config (HF_TOKEN, OLLAMA_HOST overrides). Never committed.
.env
.env.*
!.env.example
