# Work history

Create one `YYYY-MM-organization-role.md` file per materially distinct role.
Each file owns its dates, title, context, visibility, sources, and stable claim
IDs. Use the `resume-builder` convention reference for the record shape.
