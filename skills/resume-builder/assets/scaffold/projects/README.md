# Projects

Create one `<project-slug>.md` file per resume-relevant project. Record the
person's contribution and evidence-backed outcomes, not only what the project
does. Claims use stable IDs and explicit visibility.
