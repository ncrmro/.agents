# Publication targets

Create one `<target-slug>.md` adapter per website or other publication surface.
Record the designated variant, destination mapping, validation command,
publication boundary, and last verified publication state.
